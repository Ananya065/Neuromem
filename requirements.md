## Packages
framer-motion | Page transitions and complex memory block animations
recharts | Dashboard analytics and performance charts
lucide-react | Beautiful, consistent icons across the UI

## Notes
Tailwind Config - CSS variables map to font-family in the base config, we will use 'Outfit' for sans and 'JetBrains Mono' for mono.
Dark mode is the default and only theme for this cyber-futuristic application.
We assume all endpoints specified in routes.ts return exact JSON matching the Zod schemas.
